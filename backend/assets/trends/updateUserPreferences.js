export async function updateUserPreferences(user, book, action) {
    /*
    designed to update the user.preferencias 
    Inputs:
        user: {
            _id: str,
            nombre: str,
            iat: num,
            exp: num
        }
        book: {
            _id: str,
            ... (all the information in the book)
        }
        action: openedBook/query
    Output:
        undefined: it is just to update values
    */
}